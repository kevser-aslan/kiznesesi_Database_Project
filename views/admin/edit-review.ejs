<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yorum Düzenle</title>
    <style>
        /* Genel Stil */
        body {
            font-family: Arial, sans-serif;
            background-color: #fdf1f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            flex-direction: column;
        }

        /* Başlık */
        .page-title {
            text-align: center;
            color: #e91e63;
            margin-bottom: 20px;
        }

        /* Formun genel stili */
        .review-form {
            background-color: #ffffff;
            border: 1px solid #e91e63;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 100%;
            max-width: 400px;
            margin: 0 auto;
        }

        /* Form etiketleri */
        .form-label {
            display: block;
            margin-bottom: 5px;
            color: #333333;
            font-weight: bold;
        }

        /* Input ve Textarea */
        .form-input,
        .form-textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #cccccc;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 14px;
        }

        .form-textarea {
            resize: vertical;
            min-height: 80px;
        }

        /* Gönderim Butonu */
        .submit-button {
            width: 100%;
            background-color: #e91e63;
            color: #ffffff;
            padding: 10px;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }

        .submit-button:hover {
            background-color: #c2185b;
        }

        /* Geri Dön Linki */
        .back-link {
            display: block;
            text-align: center;
            margin-top: 15px;
            color: #e91e63;
            text-decoration: none;
            font-weight: bold;
        }

        .back-link:hover {
            color: #c2185b;
        }
    </style>
</head>
<body>
    <h1 class="page-title">Yorum Düzenle</h1>
    <form class="review-form" action="/admin/products/review/edit/<%= review.id %>" method="POST">
        <label class="form-label" for="rating">Puan:</label>
        <input class="form-input" type="number" id="rating" name="rating" value="<%= review.rating %>" min="1" max="5" required><br>
        
        <label class="form-label" for="comment">Yorum:</label>
        <textarea class="form-textarea" id="comment" name="comment" required><%= review.comment %></textarea><br>
        
        <button class="submit-button" type="submit">Yorumu Güncelle</button>
    </form>
    <br>
    <a class="back-link" href="/admin/products">Geri Dön</a>
</body>
</html>
