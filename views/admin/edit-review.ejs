<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yorum Düzenle</title>
</head>
<body>
    <h1>Yorum Düzenle</h1>
    <form action="/admin/products/review/edit/<%= review.id %>" method="POST">
        <label for="rating">Puan:</label>
        <input type="number" id="rating" name="rating" value="<%= review.rating %>" min="1" max="5" required><br>
        
        <label for="comment">Yorum:</label>
        <textarea id="comment" name="comment" required><%= review.comment %></textarea><br>
        
        <button type="submit">Yorumu Güncelle</button>
    </form>
    <br>
    <a href="/admin/products">Geri Dön</a>
</body>
</html>
