<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Giriş Yap</title>
  <style>
    /* Genel Sayfa Ayarları */
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f8f0f9;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    /* Sayfa Ortalanma */
    .main-container {
      width: 100%;
      max-width: 600px;
      padding: 20px;
      box-sizing: border-box;
      flex: 1;
    }

    /* Content Bölümü */
    .main-content {
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      padding: 40px;
      margin: 20px auto;
      text-align: center;
    }

    /* Başlık */
    .login-title {
      font-size: 36px;
      color: #e91e63;
      margin-bottom: 20px;
      font-weight: bold;
    }

    /* Form Elemanları */
    .form-login {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .form-group {
      margin-bottom: 20px;
      width: 100%;
    }

    .form-label {
      font-size: 16px;
      color: #333;
      margin-bottom: 8px;
      text-align: left;
    }

    .form-input {
      width: 100%;
      padding: 12px;
      font-size: 16px;
      border-radius: 5px;
      border: 1px solid #ccc;
      margin-top: 5px;
    }

    /* Buton Ayarları */
    .btn-login {
      width: 100%;
      padding: 12px;
      background-color: #e91e63;
      color: #fff;
      font-size: 18px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .btn-login:hover {
      background-color: #d81b60;
      transform: translateY(-3px);
    }

    /* Form Altı Metin */
    .form-text {
      font-size: 16px;
      color: #333;
    }

    .form-link {
      text-decoration: none;
      color: #e91e63;
      font-weight: bold;
    }

    .form-link:hover {
      color: #d81b60;
    }

    /* Header ve Footer Margin Ayarları */
    .header-footer-margin {
      margin-top: 10px;
      margin-bottom: 10px;
    }
  </style>
</head>
<body class="page-login">
  <%- include('partials/header') %> <!-- Header'ı dahil ediyoruz -->

  <main class="main-content">
    <h1 class="login-title">Giriş Yap</h1>
    <form action="/user/login" method="POST" class="form-login">
      <div class="form-group">
        <label for="email" class="form-label">E-posta:</label>
        <input type="email" id="email" name="email" class="form-input" required>
      </div>
      <div class="form-group">
        <label for="password" class="form-label">Şifre:</label>
        <input type="password" id="password" name="password" class="form-input" required>
      </div>
      <button type="submit" class="btn btn-login">Giriş Yap</button>
      <p class="form-text">
        Henüz hesabınız yok mu? <a href="/user/signup" class="form-link">Kayıt Ol</a>
      </p>
    </form>
  </main>

  <%- include('partials/footer') %> <!-- Footer'ı dahil ediyoruz -->
</body>
</html>
